import { PlaceholderPage } from "@/components/placeholder-page";

export default function EventsPage() {
  return (
    <PlaceholderPage
      title="Events"
      description="Create and manage events, shifts, and staff allocations for your organisation."
    />
  );
}
